<!DOCTYPE html>
<html>
<title>W3.CSS Template</title>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<style>
    body {
        font-family: "Avenir Next", Georgia, Serif;
    }

    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
        font-family: "Playfair Display";
        letter-spacing: 5px;
    }
</style>

<body>

    <!-- Navbar (sit on top) -->
    <div class="w3-top">
        <div class="w3-bar w3-white w3-padding w3-card" style="letter-spacing:4px;">
            <a href="#home" class="w3-bar-item w3-button">Book a recording session</a>
            <!-- Right-sided navbar links. Hide them on small screens -->
            <div class="w3-right w3-hide-small">
                <a href="#studios" class="w3-bar-item w3-button">Studios</a>
                <a href="#artists" class="w3-bar-item w3-button">Artists</a>
                <a href="#engineers" class="w3-bar-item w3-button">Engineers</a>
            </div>
        </div>
    </div>

    <!-- Header -->
   
            <h1 class="w3-xxlarge">View Studios</h1>
        </div>
    </header>

    <body>
    <!-- DISPLAY Zat.ALL -->

<section class="main">

            <% if @zats.blank? %>
<h2>There are currently no recording sessions scheduled.</h2>
        <% else %>
<h2>Scheduled Recording Sessions</h1>
<br>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Engineer</th>
            <th>Artist</th>
            <th>Studio</th>
            <th>Recording Session Date</th>
            <th>Complete</th>
            <th colspan="3"></th>
        </tr>
    </thead>

            <% @zats.each do |c| %>
    <tbody>
    <tr>
    <td><a href="/engineers/@engineer_id page/:id"><%= c.id %></a></td>
    <td><a href="/engineers/@engineer_id page/:id"><%= c.engineer.name.capitalize %></a></td>
    <td><a href="/artists/:artist_id"><%= c.artist.name.capitalize %></a></td>
    <td><a href="/studios/@studios_path"><%= c.studio.name.capitalize %></a></td>
    <td><a href="/zats/:id/edit"><%= c.appointment_date.time.to_formatted_s(:rfc822) %></a></td>
    <td><a href="/zats/:id/edit"><%= c.status %></a></td>
    </tr>
            <% end %>
    </tbody>
</table>

<p> <% end %> </p>

<br>
</section>

<!-- LINK TO CREATE NEW SESSION FORM -->
http://localhost:3000/zats/new

<!-- TESTING Link_to functionality -->

<h2>View Studios!</h2>

---------------------test
<table>
    <% @studios.each do |studio| %>
    <tr>
        <th> <%= link_to 'why', studio.name %> </th>
    </tr>
    <% end %>
</table>
-----------------------------------------------------------------

<!-- test code -->

<table>
    <thead>
        <tr>
            <th>Recording ID</th>
            <th>Recording Date</th>
            <th>Status</th>
            <th>Delete</th>
            <th colspan="3"></th>
        </tr>
    </thead>

    <% @zats.each do |zat| %>
    <tbody>
        <tr>
            <td><a href='/zats/:id'><%= zat.id %></a></td>
            <td><a href="/zats/:artist_id"><%= zat.appointment_date.time.to_formatted_s(:rfc822) %></a></td>
            <td><a href="/zats/:artist_id"><%= zat.status %></a></td>
            <td><a href="/zats/:artist_id"><%= link_to "Delete", zat, method: :delete, data: { confirm: "Really?" } %></a></td>
            
        </tr>
        <% end %>
    </tbody>
</table>